# Configuration

AIC reads runtime configuration from environment variables with the `AIC_` prefix. Copy the appropriate example file to a local `.env` file or inject variables through the runtime environment. Real `.env` files are ignored by Git.

Supported environments are `development`, `testing`, and `production` through `AIC_ENVIRONMENT`.
